<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dsc.app.reckon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="dsc.app.reckon.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.activity.AppIntro"
            android:theme="@style/AppTheme_NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.AppEntryPoint"
            android:theme="@style/AppTheme_NoActionBar" />
        <activity
            android:name=".eCommerce.ECommerceHost"
            android:label="@string/title_activity_in_cart"
            android:theme="@style/AppTheme_NoActionBar" />
        <activity
            android:name=".eCommerce.fragmentsUi.OrderNow"
            android:label="@string/title_activity_order_now"
            android:theme="@style/AppTheme_NoActionBar"></activity>
    </application>

</manifest>